<h1>Lista de cervezas - Cliente Ruby</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @beers.each do |beer| %>
      <tr>
        <td><%= beer.name %></td>
        <td><%= beer.description %></td>
        <td><%= link_to 'Ver', beer %></td>
        <td><%= link_to 'Editar', edit_beer_path(beer) %></td>
        <td><%= link_to 'Eliminar', beer, method: :delete, data: { confirm: '¿Está seguro(a)?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Cerveza', new_beer_path %>
